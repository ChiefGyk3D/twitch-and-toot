######################################################################
# SECURITY NOTE:
# All package versions are locked due to supply chain attacks (e.g. Shai-Hulud)
# See: https://www.cisa.gov/news-events/alerts/2025/09/23/widespread-supply-chain-compromise-impacting-npm-ecosystem
# You may update versions to newer libraries as desired, but auto-picking versions
# is NOT allowed for security and stability reasons.
#
# Keep this file in sync with the repository root requirements.txt.
######################################################################
# Shared core: social posting (Bluesky/Mastodon/Discord/Matrix), LLM
# generation (Ollama/Gemini with failover + guardrails), config/secrets.
# Extras pull the pinned-in-hypeman SDKs: atproto, grapheme, Mastodon.py,
# ollama, google-genai, beautifulsoup4, boto3, hvac, doppler-sdk.
# TODO: switch to `hypeman-social[all,aws,vault,doppler]>=0.1.0` once on PyPI.
hypeman-social[all,aws,vault,doppler] @ https://github.com/ChiefGyk3D/hypeman/archive/126c28b5da4901e8ca102955193b71652ec9d348.tar.gz

twitchAPI==4.5.0
python-dotenv==1.2.2
google-api-python-client==2.193.0
# CVE-2025-4565 fix: Requires protobuf >= 4.25.8 (v4), >= 5.29.5 (v5), or >= 6.31.1 (v6)
# Using v4 for compatibility; upgrade to v5/v6 requires testing
protobuf==7.34.1
# CVE-2024-45590, CVE-2024-42473: Fixed in requests >= 2.32.2
# CVE-2024-6472: Fixed in requests >= 2.32.4
# Using latest stable version 2.32.5
requests==2.33.1
# NOTE: discord.py and matrix-nio removed — never imported; Discord uses
# webhooks and Matrix uses the client-server HTTP API via requests.
# CVE-2024-5569: Infinite loop in zipp, fixed in >= 3.19.1
zipp==3.23.0
# CVE-2024-37891, CVE-2024-37080: Fixed in urllib3 >= 2.2.2
# CVE-2025-78866: Open redirect fixed in urllib3 >= 2.5.0
# GHSA-qccp-gfcp-xxvc (header leak on proxied redirects) & GHSA-mf9v-mfxr-j63j
# (decompression-bomb bypass): both fixed in urllib3 >= 2.7.0
# Using latest stable v2.x (v1.26.19 is legacy Python 2.7 support)
urllib3==2.7.0
# pip symbolic link extraction vulnerability fix
# Ensures pip >= 24.2 for CVE fixes
# GHSA-58qw-9mgm-455v, GHSA-jp4c-xjxw-mgf9, GHSA-wf93-45jw-7689: fixed in pip >= 26.1.2
pip==26.1.2
# Testing framework
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==7.1.0
